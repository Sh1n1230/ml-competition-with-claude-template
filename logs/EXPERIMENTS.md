# EXPERIMENTS

> `/setup` 実行後にコンペ情報で置き換える。**スコアが出たら必ず1行追加する。**
> 記録していない実験は存在しなかったのと同じ。

Metric: `<metric>` (`<Maximize|Minimize>`) / CV: `<fold scheme>`, n_folds=`<n>`, seed=`<seed>`

| # | date | what changed | model / config | CV | LB(public) | 判定 | notes |
|---|------|--------------|----------------|----|-----------|------|-------|
| 1 | YYYY-MM-DD | baseline | | | | keep | 起点 |

判定: `keep` 採用 / `drop` 不採用 / `hold` 保留（要再検証）

## 効かなかったこと（再試行禁止リスト）

- 

## 現在のベスト

- 構成: 
- CV / LB: 
- 生成コマンド: 
